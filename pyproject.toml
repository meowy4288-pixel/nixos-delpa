[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "aider"
version = "1.0.0"
description = "Task-first private coding agent — scan, patch, test, repeat."
requires-python = ">=3.10"

[project.scripts]
aider = "aider.main:main"
